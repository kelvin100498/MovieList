import React from 'react';
import {StyleSheet, Text, View} from 'react-native';
import {COLOR} from '../../config/color';

const eksplor = () => {
  return (
    <View>
      <Text style={{color: COLOR.white}}>ini eksplor</Text>
    </View>
  );
};

export default eksplor;

const styles = StyleSheet.create({});
