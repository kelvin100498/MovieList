import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const profileDetail = () => {
  return (
    <View>
      <Text>ini Profile Detail</Text>
    </View>
  );
};

export default profileDetail;

const styles = StyleSheet.create({});
